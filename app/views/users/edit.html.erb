<h1>Edit Profile</h1>

<%= form_for(@user, html: {multipart: true}) do |f| %>
	<%= f.label :username %>
	<%= f.text_field :username %>

	<%= f.label :bio %>
	<%= f.text_area :bio %>

	<%= f.file_field :avatar %>

	<%= f.submit "Save Changes" %>
<% end %>